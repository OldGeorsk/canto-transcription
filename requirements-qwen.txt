# Optional dependencies for Qwen3-ASR backend
# Install with: pip install -r requirements-qwen.txt
#
# These are only needed when running with --backend qwen.
# The default --backend whisperx requires only requirements.txt.

torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.45.0
accelerate>=0.26.0
qwen-asr
librosa
